#!/bin/csh
# usage: covs
echo '================================================================='
foreach file (*.?cov)
	echo $file
	grep "#####" $file | grep -v "__dev" | grep -v "__major" | \
	    grep -v "#####:[ ]*[0-9]*:[{}]"
	echo '================================================================='
	end
