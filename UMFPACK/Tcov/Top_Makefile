#-------------------------------------------------------------------------------
# UMFPACK and AMD top-level makefile (for Tcov tests)
#-------------------------------------------------------------------------------

default: library

include ../SuiteSparse_config/SuiteSparse_config.mk

library:
	( cd Source ; $(MAKE) )
	( cd Demo   ; $(MAKE) )
